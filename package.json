{
    "name": "haps",
    "version": "0.1.0",
    "description": "",
    "scripts": {
        "build": "npx prettier . --check && tsc && cp -va src/. dist/ && rm -v dist/*.ts",
        "build:fix": "npx prettier . --write && tsc && cp -va src/. dist/ && rm -v dist/*.ts"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.243"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/jessenmorten/haps.git"
    },
    "keywords": [],
    "author": "Morten Jessen",
    "license": "MIT",
    "bugs": {
        "url": "https://github.com/jessenmorten/haps/issues"
    },
    "homepage": "https://github.com/jessenmorten/haps#readme",
    "prettier": {
        "tabWidth": 4,
        "semi": true,
        "singleQuote": false,
        "trailingComma": "all"
    }
}
